@using System.Data;

@{
    ViewBag.Title = "Creación de Condicion";
    ViewBag.Description = "Crear una nueva Condicion.";
    Layout = "";

}
<div class="row">
    <div class="col-xs-12 col-md-12">
        <div class="widget">
            <div class="widget-header bordered-bottom bordered-yellow">
                <span class="widget-caption">
                    @*[
                        @{
                            if (Model.Id_Sistema == null) {
                                var IDactivo = Model.ID_ACTIVO;


                            }
                            else{ var IDactivo = 0; }
                         }
                            ]*@ Creación de Proveedor
                </span>

                <div class="widget-buttons">
                    @*<a href="#" data-toggle="maximize">
                            <i class="fa fa-expand"></i>
                        </a>
                        <a href="#" data-toggle="collapse">
                            <i class="fa fa-minus"></i>
                        </a>
                        <a href="#" data-toggle="dispose">
                            <i class="fa fa-times"></i>
                        </a>*@
                </div>
            </div>
            <div class="widget-body">

               
                @*<div class="form-group">
                    <label class="col-lg-4 control-label">Empresa </label>
                    <div class="col-lg-8">*@
                        <!--    <select id="rolesGroup" name="rolesGroup" class="form-control" multiple="multiple" style="width: 100%;">
            @*@{
                DataTable dt = null;
                dt = (ViewBag.Empresas != null) ? (DataTable)ViewBag.Empresas : null;
            }
            @if (dt != null && dt.Rows.Count > 0)
            {
                foreach (DataRow dr in dt.Rows)
                {
                    <option value="@(dr["Id"].ToString())">@(dr["Nombre"].ToString())</option>
                }
            }*@


        </select>-->
                         @*asdasdasdsa
                    </div>
                </div>*@



                <div class="row">
                    <div class="col-sm-2">
                        <div class="form-group">
                            Alias
                        </div>
                    </div>

                    <div class="col-sm-10">
                        <div class="form-group">
                            <span class="input-icon icon-right">
                                <input id="dgAlias" type="text" class="form-control" autocomplete="Off" name="dgAlias" value="" placeholder="Rut de la Empresa"
                                       data-bv-notempty="true"
                                       data-bv-notempty-message="El campo es Obligatorio" />
                                <i class="glyphicon glyphicon-comment"></i>
                            </span>
                        </div>
                    </div>
                </div>

             

               

               


            



                <div class="row">
                    <div class="col-sm-12">
                        <div class="form-group">
                            <table align="center">
                                <tr>
                                    <td align="center">

                                        @*@if (User.IsInRole("ope_compania_crear")) *@
                                        @*{*@
                                        <button id="btnAgregar" class="btn btn-primary" onclick="GrabarProveedorBD( dgRut, dgAlias, dgNombre, dgFono, dgDireccion, dgCorreo, dgCodEstado, btnAgregar)"><i class="fa fa-floppy-o"></i>Grabar</button>


                                        @*}*@
                                        @*else*@
                                        @*{*@
                                        @*<button id="btnAgregar" class="btn-primary" disabled onclick="GrabarAdjuntoBD(dgDescripcion,NomArchivo,AtachTable,btnAgregar)><i class="fa fa-folder-open-o"></i>Grabar</button>*@
                                        @* } *@

                                        @*@if (User.IsInRole("ope_compania_crear")) *@
                                        @*{*@
                                        <button id="btnVolver" class="btn btn-primary" data-dismiss="modal"><i class="fa fa-undo"></i>Volver</button>

                                        @*}*@
                                        @*else*@
                                        @*{*@
                                        @*<button disabled id="btnVolver" class="btn-primary" data-dismiss="modal"><i class="fa fa-folder-open-o"></i>Archivo</button>*@
                                        @* } *@

                                    </td>
                                </tr>
                            </table>

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@{
    var relativeUrl = HttpContext.Current.Request.Url.Segments;
    var url = string.Empty;

    foreach (var item in relativeUrl)
    {
        if (!item.Equals("Attach"))
        {
            url += item;
        }
        else
        {
            break;
        }
    }
}
@section PageScripts{
    <script src="~/assets/js/validation/bootstrapValidator.js"></script>
    <script src="~/assets/js/select2/select2.js"></script>
    <script src="~/assets/js/bootbox/bootbox.js"></script>
    <script src="~/assets/js/datatable/jquery.dataTables.min.js"></script>


    <script type="text/javascript" class="init">

    </script>
}
